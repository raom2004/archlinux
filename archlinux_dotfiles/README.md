# dotfiles
User-specific application configuration files whose filename starts with a dot. This Git repo is intended to track dotfiles directly, using the method "bare repository and alias method" described in https://wiki.archlinux.org/title/Dotfiles.
